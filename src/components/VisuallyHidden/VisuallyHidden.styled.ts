import styled from 'styled-components';

export const Span = styled.span`
  display: inline-block;
  position: absolute;
  overflow: hidden;
  clip: rect(0 0 0 0);
  clip-path: inset(50%);
  height: 1px;
  width: 1px;
  margin: -1;
  padding: 0;
  border: 0;
`;
