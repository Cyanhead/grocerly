import FirebaseContext from './context';
import Firebase, {
  db,
  colRef,
  productsImagesRef,
} from './firebase';

export default Firebase;
export {
  db,
  FirebaseContext,
  colRef,
  productsImagesRef,
};
